# Demo Auth API

This demo repository is a small backend-style Python codebase designed for RepoPilot analysis. It contains a realistic login flow, a clear rate-limiting insertion point on the login route, and an existing email service placeholder that could support a future email verification feature.

The repository intentionally does **not** implement login rate limiting or email verification yet. Those gaps are deliberate so the locked demo prompts have clear, inspectable answers.
